# Plan Template

Full structural contract for `plan.md` inside `release-plans/plan_NN_<slug>/`.

```markdown
---
sequence: NN
target-version: vX.Y.Z
title: "Release plan NN: <theme>"
type: release-plan
status: draft                    # draft -> in-progress -> released
created: YYYY-MM-DD
updated: YYYY-MM-DD
target-date: YYYY-MM-DD          # optional
includes: []                     # list of effort ids (e.g., [S-07, S-05]); grows on promote
spec-count: 0                    # computed by --update
plan-count: 0                    # computed by --update
checklist-complete: false        # computed by --update
---

# Release Plan NN: <theme>

## Theme

<one-line release theme from --theme, or "vX.Y.Z release" by default>

## Context

(Optional maintainer-authored paragraph describing why this release exists, what changed since the last release, any external constraints.)

---

## Aggregation

Generated by `--update`. Do not hand-edit rows.

| id | title | spec-status | plan-status | AC-coverage | has-plan? |
|----|-------|-------------|-------------|-------------|-----------|

(Empty until efforts are promoted. After --promote, each effort folder contributes one row.)

---

## Hygiene Gates

These conditions block tagging. `--gate` reports current pass/fail.

| Gate | Condition | Status |
|------|-----------|--------|
| (a) Spec status | Every effort's `spec.md` has `status: committed` or `fulfilled` (no `draft`) | N/A (no efforts yet) |
| (b) Coupled plan | Every effort has an `implementation-plan.md`, OR is in `gate-waivers` frontmatter | N/A |
| (c) AC coverage | Every `implementation-plan.md` has `ac-coverage: complete` | N/A |
| (d) Phases done | Every plan's completion table shows every phase `Done` | N/A |
| (e) Staleness | No `spec.md` edited after its sibling `implementation-plan.md`'s last edit | N/A |

(See `references/hygiene-gates.md` for the computation rules per gate.)

---

## Doc-Update Checklist

Every box must be checked before the release tag. The list combines built-in defaults with project-level extensions from `docs/internal/release-plans/release-checklist.yaml` (see `references/checklist-extension.md`).

| Doc | Update | Done |
|-----|--------|------|
| `CHANGELOG.md` | Move items from [Unreleased] to vX.Y.Z section | [ ] |
| `README.md` | Bump any version references | [ ] |
| `AGENTS.md` | Reflect new or renamed skills | [ ] |
| `docs/skills/README.md` | Sync per-skill version table | [ ] |
| `skills/*/HISTORY.md` | Append the version's changes (per affected skill) | [ ] |
| `.claude-plugin/plugin.json` | Bump plugin `version` | [ ] |
| `skills/*/SKILL.md` | Bump per-skill `version` frontmatter (per affected skill) | [ ] |
| Git tag `vX.Y.Z` | Create the annotated tag once all of the above are done | [ ] |

<!-- Additional rows from release-checklist.yaml appear here. -->

---

## Open Questions / Decisions

Release-level open questions and decisions (scope cuts, slips, blockers, deferrals, new issues filed). Uses the decisions-section standard in `references/decisions-section.md`. If none, write "None at this time."

| ID | Title | Resolution | Status | Updated |
|----|-------|------------|--------|---------|
| D1 | <short handle> | (none) | Open | (none) |

### D1: <Title> (Open)

**Summary.** One-line restatement.

**Context.** Why it's open, the background, why it matters.

**Desired outcome.** What good looks like, independent of which option wins.

**Options / approaches.**

* **Option A:** <description + key trade-off>
* **Option B:** <description + key trade-off>

**Recommendation.** <Pick + 1 to 3 sentence rationale.>

---

> **Maintainer decision:** _(pending)_
>
> * **Status:** Open
> * **Choice:** (none)
> * **Reasoning:** (none)
> * **Decided by / date:** (none)

---

## Notes

(Maintainer-authored. Miscellaneous context that does not fit elsewhere. Record decisions in the Open Questions / Decisions section above.)
```

## Template notes

- **Folder structure:** the .md sits at `release-plans/plan_NN_<slug>/plan.md`. Per-effort subfolders are peers (siblings to the .md inside the folder).
- **Aggregation section is generated.** `--update` rewrites this section's table from the folder walk. Don't author the rows.
- **Hygiene gates section is partially generated.** The gate descriptions are static (template above); the `Status` column is updated by `--gate` (read-only) and by `--update` (incidental).
- **Doc-update checklist defaults are baked in** but the `release-checklist.yaml` adds project-specific rows on top. Per-release tweaks happen by hand-editing the table after `--create`.
- **Theme / Context / Notes sections are maintainer-authored.** `--update` does not touch them.

## What goes where, decision tree

```
Is this content about... ?
  - what's in scope (specs and plans)  -> Aggregation (generated, do not edit)
  - whether the release can ship       -> Hygiene Gates (status auto, descriptions static)
  - what docs need updating            -> Doc-Update Checklist (defaults + yaml extension)
  - why this release exists            -> Theme / Context (maintainer-authored)
  - decisions made about the release   -> Open Questions / Decisions (decisions-section standard)
  - the spec or plan itself            -> NOT HERE. Lives in the per-effort folder.
```
