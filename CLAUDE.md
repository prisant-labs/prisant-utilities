# CLAUDE.md

This repository's agent instructions live in `AGENTS.md`. That file is the cross-harness convention and is what Codex and other agents read; this file exists only so Claude Code loads the same instructions rather than running without them.

Keep the content in `AGENTS.md`. Leave this file as the pointer, so the two can never disagree.

@AGENTS.md
